# Micro-ESPectre Requirements
# Python dependencies for Micro-ESPectre CLI and analysis tools

# Device Management Dependencies
esptool==5.1.0          # Flash firmware to ESP32 (pinned for ESPHome 2026.2.0 compatibility)
mpremote>=1.26.1        # Deploy code to MicroPython device (used as CLI tool)
pyserial>=3.5           # Serial port detection and communication

# CLI Tool Dependencies
paho-mqtt==1.6.1        # MQTT client (pinned for ESPHome compatibility)
colorama>=0.4.6         # Terminal colors
python-dotenv>=1.2.1    # Environment variables support
PyYAML>=6.0.3           # YAML parsing
prompt_toolkit>=3.0.0   # Interactive CLI with autocompletion

# Analysis Tools Dependencies
numpy>=2.3.5            # Numerical computing for data analysis
matplotlib>=3.10.7      # Plotting and visualization
scipy>=1.16.3           # Scientific computing (signal processing)
PyWavelets>=1.9.0       # Wavelet transforms (pywt)

# Test Dependencies
pytest>=8.0.0           # Test framework
pytest-cov>=4.1.0       # Coverage plugin for pytest

# Code Coverage (C++ tests)
gcovr>=8.4              # Code coverage reports for C++ tests

# ML Dependencies
tensorflow>=2.20.0      # Machine learning framework
scikit-learn>=1.8.0     # Machine learning library
shap>=0.46.0            # SHAP feature importance analysis